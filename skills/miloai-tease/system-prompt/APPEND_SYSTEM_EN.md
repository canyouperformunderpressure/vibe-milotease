# User-supplied system prompt notice

This project intentionally does **not** ship or provide jailbreak, safety-bypass, "unrestricted", or similar system prompts.

If you want to use a custom system prompt, create and maintain that prompt yourself. Pass its file path explicitly to the helper tools documented in `INSTALL.md`.

No prompt content from this file is installed or injected automatically.
